# 階段 3: 前端開發 (快速原型)
# Frontend Development with Rapid Prototyping

**預計時間**: 1 小時
**難度**: ★★☆☆☆ (中等偏易)
**前置要求**: 完成階段 2 (後端 API 開發)
**核心技能**: React 基礎、API 整合、狀態管理、響應式設計

---

## 📋 階段目標

完成這個階段後,你將擁有:

✅ **完整的用戶介面** - 登入、註冊、任務管理、Dashboard
✅ **與後端 API 整合** - 所有功能可正常呼叫後端
✅ **身份驗證流程** - JWT token 管理、自動登入
✅ **響應式設計** - 支援桌面與行動裝置
✅ **良好的 UX** - Loading 狀態、錯誤提示、成功反饋

**成功標準**:
- 能完成完整的用戶流程 (註冊 → 登入 → 創建任務 → 查看 Dashboard)
- UI 美觀且易用 (使用 Tailwind CSS)
- 所有 API 呼叫都有適當的錯誤處理
- 前端編譯無錯誤 (`npm run build` 成功)

---

## 🎯 為什麼選擇 React + Tailwind？

### 快速開發的最佳組合

**React**:
- ✅ 組件化開發，代碼可復用
- ✅ 生態系統豐富 (Axios, React Router)
- ✅ AI 工具支援度最高 (Cursor, GitHub Copilot)

**Tailwind CSS**:
- ✅ 無需寫 CSS 檔案，直接在 HTML 中寫樣式
- ✅ 快速原型開發，1 小時內完成 UI
- ✅ 響應式設計簡單 (`md:`, `lg:` 前綴)

**替代方案**:
如果你更熟悉 Vue.js，可替換為 Vue 3 + Tailwind，流程類似。

---

## 🏗️ 前端架構總覽

```
frontend/
├── src/
│   ├── components/              # React 組件
│   │   ├── Auth/
│   │   │   ├── LoginForm.jsx    # 登入表單
│   │   │   └── RegisterForm.jsx # 註冊表單
│   │   ├── Dashboard/
│   │   │   ├── StatsCard.jsx    # 統計卡片
│   │   │   └── PriorityChart.jsx # 優先級圖表
│   │   ├── Tasks/
│   │   │   ├── TaskList.jsx     # 任務列表
│   │   │   ├── TaskCard.jsx     # 單一任務卡片
│   │   │   ├── TaskForm.jsx     # 創建/編輯表單
│   │   │   └── TaskFilters.jsx  # 篩選器
│   │   └── Layout/
│   │       ├── Navbar.jsx       # 導航欄
│   │       └── Layout.jsx       # 頁面布局
│   │
│   ├── pages/                   # 頁面組件
│   │   ├── AuthPage.jsx         # 登入/註冊頁
│   │   ├── DashboardPage.jsx    # Dashboard
│   │   └── TasksPage.jsx        # 任務管理頁
│   │
│   ├── services/                # API 服務
│   │   ├── api.js               # Axios 實例配置
│   │   ├── authService.js       # 認證相關 API
│   │   └── taskService.js       # 任務相關 API
│   │
│   ├── contexts/                # React Context
│   │   └── AuthContext.jsx      # 認證狀態管理
│   │
│   ├── utils/                   # 工具函數
│   │   ├── tokenManager.js      # Token 存取
│   │   └── dateFormatter.js     # 日期格式化
│   │
│   ├── App.jsx                  # 根組件
│   └── index.jsx                # 入口文件
│
├── public/
│   └── index.html
│
├── package.json
├── tailwind.config.js
└── vite.config.js (或 webpack.config.js)
```

---

## ⏱️ 時間分配建議

```
總時間: 60 分鐘

子階段 3.1: 專案初始化與設置 (15 min)
   ├─ 3.1.1 創建 React 專案 (5 min)
   ├─ 3.1.2 安裝依賴與配置 Tailwind (5 min)
   └─ 3.1.3 設置 API 服務層 (5 min)

子階段 3.2: 核心組件開發 (35 min)
   ├─ 3.2.1 認證頁面 (登入/註冊) (10 min)
   ├─ 3.2.2 任務列表與 CRUD (15 min)
   ├─ 3.2.3 Dashboard 統計頁面 (10 min)

子階段 3.3: 整合與測試 (10 min)
   ├─ 3.3.1 整合測試 (5 min)
   ├─ 3.3.2 UI/UX 優化 (3 min)
   └─ 3.3.3 建置與驗證 (2 min)
```

---

## 📝 詳細實作步驟

### 子階段 3.1: 專案初始化與設置 (15 min)

#### 3.1.1 創建 React 專案 (5 min)

**Step 1: 使用 Vite 創建專案**

```bash
# 在專案根目錄下
npm create vite@latest frontend -- --template react
cd frontend
npm install
```

**為什麼選擇 Vite？**
- ⚡ 啟動速度極快 (比 Create React App 快 10 倍)
- 🔥 熱更新即時反饋
- 📦 建置輸出更小

**AI 協作提示詞**:
```
使用 Vite 創建一個 React 專案，專案名稱為 "frontend"。
配置要求:
1. React 18+
2. 使用 JSX (不用 TypeScript，MVP 階段簡化)
3. 確保專案能正常運行

指令輸出後告訴我下一步。
```

---

#### 3.1.2 安裝依賴與配置 Tailwind (5 min)

**Step 1: 安裝核心依賴**

```bash
# 安裝 Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 安裝其他依賴
npm install axios react-router-dom
```

**Step 2: 配置 Tailwind**

**AI 協作提示詞**:
```
配置 Tailwind CSS 到我的 Vite + React 專案:

1. 修改 tailwind.config.js，設置 content 路徑為:
   ["./index.html", "./src/**/*.{js,jsx}"]

2. 在 src/index.css 中添加 Tailwind 指令:
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

3. 確保 src/main.jsx 有 import './index.css'

4. 提供一個簡單的測試代碼讓我驗證 Tailwind 是否生效
```

**驗證 Tailwind**:
```jsx
// src/App.jsx
function App() {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <h1 className="text-4xl font-bold text-blue-600">
        Tailwind CSS 已生效！
      </h1>
    </div>
  );
}

export default App;
```

執行 `npm run dev`，訪問 `http://localhost:5173`，應該看到藍色大標題。

---

#### 3.1.3 設置 API 服務層 (5 min)

**Step 1: 創建 Axios 實例**

**檔案**: `src/services/api.js`

**AI 協作提示詞**:
```
創建一個 Axios API 服務層，檔案路徑 src/services/api.js

要求:
1. 基礎 URL: http://localhost:8000/api (可從環境變數讀取)
2. 自動在 headers 中添加 JWT token (從 localStorage 讀取)
3. 處理 401 錯誤 (token 失效時清除並跳轉登入頁)
4. 導出配置好的 axios 實例

提供完整代碼。
```

**預期產出**:
```javascript
// src/services/api.js
import axios from 'axios';

const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api';

const api = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Request interceptor: 自動添加 token
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// Response interceptor: 處理錯誤
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

**Step 2: 創建認證服務**

**檔案**: `src/services/authService.js`

**AI 協作提示詞**:
```
創建認證服務，檔案 src/services/authService.js

功能:
1. register(email, password, username) - 註冊
2. login(email, password) - 登入，成功後保存 token 到 localStorage
3. logout() - 登出，清除 token
4. getCurrentUser() - 獲取當前用戶 (GET /auth/me)

使用我們已創建的 api 實例 (從 './api.js' 導入)
每個函數返回 Promise

提供完整代碼。
```

---

### 子階段 3.2: 核心組件開發 (35 min)

#### 3.2.1 認證頁面 (登入/註冊) (10 min)

**目標**: 創建一個同時包含登入和註冊表單的頁面

**Step 1: 創建 AuthContext**

**檔案**: `src/contexts/AuthContext.jsx`

**AI 協作提示詞**:
```
創建 React Context 用於認證狀態管理，檔案 src/contexts/AuthContext.jsx

功能:
1. 狀態: user (當前用戶), isAuthenticated (是否已登入), loading
2. 方法: login, register, logout
3. 初始化時從 localStorage 檢查 token 並獲取用戶資訊
4. 提供 AuthProvider 和 useAuth hook

使用我們的 authService

提供完整代碼。
```

---

**Step 2: 創建登入/註冊表單**

**檔案**: `src/pages/AuthPage.jsx`

**AI 協作提示詞**:
```
創建認證頁面，檔案 src/pages/AuthPage.jsx

設計要求:
1. 兩個 Tab: "登入" 和 "註冊"
2. 登入表單: Email, Password, 登入按鈕
3. 註冊表單: Email, Password, Username, 註冊按鈕
4. 表單驗證 (基本的 required 和 email 格式)
5. 錯誤提示 (紅色文字)
6. 成功後跳轉 /dashboard

使用 Tailwind CSS 美化，風格簡潔現代。
使用 useAuth hook (從 AuthContext)

提供完整代碼。
```

**預期 UI 效果**:
```
┌────────────────────────────────┐
│  Task Management System        │
│                                │
│  ┌─────┐  ┌─────┐             │
│  │登入 │  │註冊│              │  (Tab 切換)
│  └─────┘  └─────┘             │
│                                │
│  Email                         │
│  ┌──────────────────────────┐ │
│  │ user@example.com         │ │
│  └──────────────────────────┘ │
│                                │
│  Password                      │
│  ┌──────────────────────────┐ │
│  │ ••••••••                 │ │
│  └──────────────────────────┘ │
│                                │
│      ┌──────────┐              │
│      │   登入   │              │
│      └──────────┘              │
└────────────────────────────────┘
```

---

#### 3.2.2 任務列表與 CRUD (15 min)

**Step 1: 創建任務服務**

**檔案**: `src/services/taskService.js`

**AI 協作提示詞**:
```
創建任務服務，檔案 src/services/taskService.js

功能 (使用 api 實例):
1. getTasks(params) - 獲取任務列表，params: { status, priority, sort_by, page, limit }
2. getTask(id) - 獲取單一任務
3. createTask(data) - 創建任務
4. updateTask(id, data) - 更新任務
5. deleteTask(id) - 刪除任務
6. getDashboardStats() - 獲取統計數據 (GET /dashboard/stats)

提供完整代碼。
```

---

**Step 2: 創建任務列表頁面**

**檔案**: `src/pages/TasksPage.jsx`

**AI 協作提示詞**:
```
創建任務管理頁面，檔案 src/pages/TasksPage.jsx

功能:
1. 顯示任務列表 (卡片形式)
2. 篩選器: 狀態 (TODO/IN_PROGRESS/DONE), 優先級
3. 排序: 創建時間、優先級、截止日期
4. 創建任務按鈕 (彈出表單 modal 或跳轉新頁面)
5. 每個任務卡片顯示:
   - 標題
   - 優先級標籤 (顏色: URGENT紅, HIGH橙, MEDIUM黃, LOW綠)
   - 狀態標籤
   - 截止日期
   - 編輯/刪除按鈕

6. Loading 狀態
7. 空狀態 (無任務時顯示提示)

使用 Tailwind CSS，響應式設計 (手機上單列，桌面上多列)

提供完整代碼。
```

---

**Step 3: 創建任務表單組件**

**檔案**: `src/components/Tasks/TaskForm.jsx`

**AI 協作提示詞**:
```
創建任務表單組件，檔案 src/components/Tasks/TaskForm.jsx

Props:
- task (可選，編輯模式時傳入)
- onSave(data) - 保存回調
- onCancel() - 取消回調

欄位:
1. 標題 (必填, max 200 字元)
2. 描述 (textarea, 可選)
3. 優先級 (下拉選單: LOW/MEDIUM/HIGH/URGENT)
4. 狀態 (下拉選單: TODO/IN_PROGRESS/DONE)
5. 截止日期 (日期選擇器)

驗證:
- 標題必填
- 截止日期不能是過去

使用 Tailwind CSS 美化

提供完整代碼。
```

---

#### 3.2.3 Dashboard 統計頁面 (10 min)

**檔案**: `src/pages/DashboardPage.jsx`

**AI 協作提示詞**:
```
創建 Dashboard 頁面，檔案 src/pages/DashboardPage.jsx

功能:
1. 呼叫 taskService.getDashboardStats() 獲取統計數據
2. 顯示 4 個統計卡片:
   - 總任務數
   - 待辦任務 (TODO)
   - 進行中任務 (IN_PROGRESS)
   - 已完成任務 (DONE)

3. 卡片設計:
   - 圓角、陰影
   - 大數字 + 標題
   - 使用不同顏色區分 (藍、黃、橙、綠)

4. 顯示優先級分佈 (簡單的文字列表或進度條):
   - URGENT: X 個
   - HIGH: X 個
   - MEDIUM: X 個
   - LOW: X 個

5. 即將到期提示 (如果有)

使用 Tailwind CSS，響應式設計 (手機單列，桌面 2x2 網格)

提供完整代碼。
```

---

### 子階段 3.3: 整合與測試 (10 min)

#### 3.3.1 設置路由與布局 (5 min)

**Step 1: 創建主布局組件**

**檔案**: `src/components/Layout/Layout.jsx`

**AI 協作提示詞**:
```
創建主布局組件，檔案 src/components/Layout/Layout.jsx

功能:
1. 頂部導航欄:
   - Logo/標題 (左側)
   - 用戶名稱 (右側)
   - 登出按鈕 (右側)
2. 導航選單:
   - Dashboard
   - 任務管理
3. 內容區域 (props.children)

使用 Tailwind CSS，固定頂部導航

提供完整代碼。
```

---

**Step 2: 配置路由**

**檔案**: `src/App.jsx`

**AI 協作提示詞**:
```
配置 React Router，檔案 src/App.jsx

路由:
1. / - 重定向到 /dashboard (已登入) 或 /login (未登入)
2. /login - AuthPage
3. /dashboard - DashboardPage (需要認證)
4. /tasks - TasksPage (需要認證)

使用 AuthContext 判斷認證狀態
未登入訪問保護路由時重定向到 /login

提供完整代碼 (包含 AuthProvider 包裹)
```

---

#### 3.3.2 整合測試 (3 min)

**測試清單**:

```bash
# 1. 啟動後端 (確保在 8000 端口運行)
cd backend
uvicorn app.main:app --reload

# 2. 啟動前端
cd frontend
npm run dev
```

**手動測試流程**:
1. ✅ 訪問 `http://localhost:5173`，自動跳轉到登入頁
2. ✅ 註冊新用戶，成功後自動登入並跳轉到 Dashboard
3. ✅ Dashboard 顯示統計數據 (初始為 0)
4. ✅ 點擊「任務管理」，進入任務列表頁
5. ✅ 創建新任務，任務顯示在列表中
6. ✅ 編輯任務，修改狀態和優先級
7. ✅ 刪除任務
8. ✅ 回到 Dashboard，統計數據更新正確
9. ✅ 登出，跳轉回登入頁

**常見問題檢查**:
- ❌ CORS 錯誤 → 確認後端 CORS 設置包含 `http://localhost:5173`
- ❌ 401 錯誤 → 檢查 token 是否正確保存到 localStorage
- ❌ 404 錯誤 → 確認後端 API 路徑正確

---

#### 3.3.3 UI/UX 優化 (2 min)

**快速優化項目**:

1. **添加 Loading 狀態**
```jsx
{loading ? (
  <div className="flex justify-center items-center h-64">
    <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-500"></div>
  </div>
) : (
  // 內容
)}
```

2. **錯誤提示優化**
```jsx
{error && (
  <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
    {error}
  </div>
)}
```

3. **成功提示 (簡易版 Toast)**
```jsx
{successMessage && (
  <div className="fixed top-4 right-4 bg-green-500 text-white px-6 py-3 rounded shadow-lg">
    ✓ {successMessage}
  </div>
)}
```

---

## 🎓 學習重點總結

### 關鍵概念

1. **狀態管理**: 使用 Context API 管理全局認證狀態
2. **API 整合**: Axios interceptors 統一處理 token 和錯誤
3. **組件化**: 將 UI 拆分成可復用的組件
4. **Tailwind CSS**: 快速構建響應式 UI

### AI 協作技巧

**好的提示詞特徵**:
- ✅ 明確指定檔案路徑
- ✅ 列出所有功能需求
- ✅ 指定使用的技術/庫
- ✅ 要求提供完整代碼

**範例**:
```
創建任務列表組件，檔案 src/components/Tasks/TaskList.jsx

使用:
- React hooks (useState, useEffect)
- taskService.getTasks()
- Tailwind CSS

功能:
1. 顯示任務卡片
2. 支援篩選和排序
3. Loading 和空狀態

提供完整代碼。
```

---

## ✅ 階段完成檢查清單

### 功能檢查
- [ ] 用戶可以註冊和登入
- [ ] JWT token 正確保存和使用
- [ ] 可以創建、查看、編輯、刪除任務
- [ ] Dashboard 統計數據正確
- [ ] 篩選和排序功能正常
- [ ] 登出功能正常

### 代碼質量
- [ ] 無 console 錯誤
- [ ] 無 ESLint 警告 (如果配置了)
- [ ] `npm run build` 成功

### UI/UX
- [ ] 所有按鈕和表單可正常操作
- [ ] 響應式設計 (在手機和桌面都能正常顯示)
- [ ] Loading 狀態清晰
- [ ] 錯誤提示友善

---

## 🚀 下一步

恭喜完成前端開發！你現在有一個完整可用的任務管理系統了。

**接下來**:
→ 前往 `階段4_CI_CD設置/README.md` 建立自動化測試和部署流程

---

## 📚 延伸學習

### 可選優化 (如果時間充裕)

1. **狀態管理升級**
   - 使用 React Query 或 SWR 優化 API 呼叫
   - 自動快取和重新驗證

2. **UI 庫升級**
   - 使用 Headless UI 或 Radix UI 提升組件質量
   - 添加動畫 (Framer Motion)

3. **表單處理優化**
   - 使用 React Hook Form 簡化表單邏輯
   - 添加更完善的驗證

4. **測試**
   - 使用 Vitest + React Testing Library 編寫單元測試

---

**記住**: 這個階段的目標是「快速可用的 MVP」，不是「完美的 UI」。
先讓功能跑起來，後續再優化！
